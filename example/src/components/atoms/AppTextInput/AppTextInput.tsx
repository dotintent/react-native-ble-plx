import React from 'react'
import type { TextInputProps } from 'react-native'
import { StyledTextInput } from './AppTextInput.styled'

export const AppTextInput = (props: TextInputProps) => <StyledTextInput {...props} />
