<manifest package="com.polidea.reactnativeble">

    <!-- App developers should make sure that they are defining proper permissions
    in their own AndroidManifest.xml. Permissions should be declared as advised in
    https://developer.android.com/guide/topics/connectivity/bluetooth/permissions

     Library won't declare permissions here, because it's up to developers to decide which
     set of permissions suits their needs best -->

</manifest>
