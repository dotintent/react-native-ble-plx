matrix:
  include:
    - language: node_js
      node_js: 8
      script:
        - npm install
        - npm run lint
        - npm test

    - language: android
      jdk: oraclejdk8
      dist: precise
      node_js: false
      android:
        components:
          - tools
          - platform-tools
          - build-tools-26.0.2
          - android-25
          - extra-android-m2repository
        licenses:
          - 'android-sdk-license-.+'
      script:
        - cd android
        - chmod +x gradlew
        - ./gradlew assembleRelease
        - ./gradlew test

    - language: objective-c
      os: osx
      osx_image: xcode9.1
      node_js: false
      xcode_project: ios/BleClientManager/BleClientManager.xcodeproj
      script:
        - cd ios/BleClientManager
        - carthage build
        - xcodebuild -scheme BleClientManager -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO
